.content
  %h1.title.center-text Blog

  .posts.center-align
    - @posts.each do |post|
      .center-align
        = render partial: 'post', locals: { post: post }
        %br

  %br
  .center-text= link_to_homepage
