- provide :title, 'Blog'

.content
  %h1.title.center-text Blog

  .posts
    .center-text
      Pat has thoughts. They are coming soon.
    - @posts.each do |post|
      .center-align
        = render partial: 'post', locals: { post: post }
        %br

  %br
  .center-text= link_to_homepage
