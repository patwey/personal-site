!!! 5
%html
  %head
    %title= "Pat Wey | #{yield(:title)}"
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags

  %body
    .flash
      - flash.each do |type, msg|
        = content_tag :div, msg, class: "flash-#{type}"

    = yield
